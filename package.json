{
  "name": "cognisphere-desktop",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter \"./apps/*\" --parallel dev",
    "build:desktop": "pnpm -F desktop-shell build",
    "setup:backend": "cd services/backend && pip install -r requirements.txt",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.1.0",
    "electron-builder": "^24.9.1",
    "typescript": "^5.3.3",
    "wait-on": "^7.2.0"
  }
}
